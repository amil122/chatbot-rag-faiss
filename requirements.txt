# Core LangChain for RAG pipeline
langchain==0.2.11
langchain-core>=0.2.23,<0.3.0
langchain-community==0.2.6
pydantic

# Vector database (FAISS)
faiss-cpu==1.8.0.post1

# Embeddings (Sentence Transformers)
sentence-transformers==2.7.0

# OpenAI client (used for OpenRouter API compatibility)
openai==1.97.1

# FastAPI for building the /chat endpoint
fastapi==0.111.0
uvicorn==0.30.1

# Environment variable management
python-dotenv==1.0.1

# Data loaders (PDF, DOCX, etc.)
pypdf==4.2.0
python-docx==1.1.0

# Optional utilities
tqdm==4.66.4
